var a = 5;
var b = 3;

var doSomething = function(x) {

	var arr = [1,2,3];

};

doSomething();

// after doSomething exits, there are no references left to arr is deallocated by the garbage collector